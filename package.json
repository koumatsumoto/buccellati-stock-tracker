{
  "name": "buccellati-stock-tracker",
  "version": "0.1.0",
  "description": "Stock tracking application",
  "engines": {
    "node": ">=20.0.0"
  },
  "type": "module",
  "main": "src/main.ts",
  "scripts": {
    "build": "tsc",
    "start": "node --env-file=.env src/main.ts",
    "test": "vitest run --coverage",
    "fmt": "prettier --write ."
  },
  "keywords": [
    "stock",
    "tracker"
  ],
  "author": "",
  "prettier": {
    "printWidth": 150
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "playwright": "^1.51.0",
    "prettier": "^3.5.3",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0"
  }
}
